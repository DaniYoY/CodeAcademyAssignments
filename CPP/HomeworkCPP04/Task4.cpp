// Задача 04
// Дефинирайте собствена версия на библиотечната функция fmin от <cmath>, която приема два float параметъра и
// връща по-малкия от двата по стойност.

float floatMin(float x, float y)
{
    if(x < y){
        return x;
    }
    return y;
}